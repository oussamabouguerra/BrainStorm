<form method="post" action="{{ path('simple_user_delete', {'id': simple_user.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ simple_user.id) }}">
    <button class="btn">Delete</button>
</form>
